first run the python-3.10.1-amd64.exe file,
go thru setup and use normal settings EXCEPT add python to path AND disable path length limit.
once installation is complete use the installandrun.bat file the first time, then use the run.bat each time thereafter to generate a .csv file with the salary and reports the links in the 
links.txt file.
